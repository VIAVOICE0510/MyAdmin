// NewTrendForm.tsx
import React, { useState } from 'react';
import { Form, Button } from 'react-bootstrap';
import Ravand from './Ravand';

export default function NewTrendForm({ onCancel }) {
  const [name, setName] = useState('');

  return (
    <Ravand/>
  );
}
