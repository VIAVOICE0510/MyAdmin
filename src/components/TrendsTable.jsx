// TrendsTable.tsx
import React from 'react';
import { Table, Button } from 'react-bootstrap';
import Ravand from './Ravand';

export default function TrendsTable({ trends, onNewTrend }) {
  return (
    <Ravand/>
  );
}
